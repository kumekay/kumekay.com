[phases.setup]
nixPkgs = ["hugo", "nginx"]
nixpkgsArchive = "71caefce12ba78d84fe618cf61644dce01cf3a96"

[phases.build]
cmds = ["hugo --minify"]

[start]
cmd = "nginx -e /dev/stderr -c /app/nginx.conf"
